import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sheldon Roth Fine Art | Visual Engineer",
  description: "Capturing the extraordinary in nature, architecture, and life through the lens of visual storytelling. Premium limited edition fine art photography.",
  keywords: ["fine art photography", "limited edition prints", "landscape photography", "wildlife photography", "architectural photography"],
  openGraph: {
    title: "Sheldon Roth Fine Art",
    description: "Premium limited edition fine art photography",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}
